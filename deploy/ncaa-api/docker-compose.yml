services:
  ncaa-api:
    image: henrygd/ncaa-api:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NCAA_HEADER_KEY: ${NCAA_HEADER_KEY:-}
